[client-server]

!includedir /etc/mysql/conf.d/
/includedir /etc/mysql/mariadb.conf.d/

[client]
port = 3306
socket = /var/run/mysqld/mysqld.sock

[mysqld]
port = 3306
socket = /var/run/mysqld/mysqld.sock
